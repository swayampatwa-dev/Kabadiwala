export const materialRates: Record<string, number> = {PCB:410,Cable:520,Battery:145,"LCD/Display":175,CRT:45,Motor:210,"Mixed Plastic":28,Aluminium:185,Copper:720,Computer:95,Laptop:240,"Mobile Phone":380,"Other E-Waste":70};
export type Condition = "POOR"|"USED"|"GOOD";
export function estimatePrice(material:string, weight=1, condition:Condition="USED") { const base=materialRates[material]||70; const factor={POOR:.72,USED:1,GOOD:1.12}[condition]; const fair=Math.round(base*factor); return {low:Math.round(fair*.85),average:fair,high:Math.round(fair*1.15),recommendedFairPrice:fair,totalLow:Math.round(fair*.85*weight),totalHigh:Math.round(fair*1.15*weight),confidence:88,trend: material==="PCB"?8.2:4.7,reasons:["Local market average","Current recycler offers","Historical prices","Material condition","Location"]}; }
export function anomaly(quote:number, benchmark:number, threshold=30){const deviation=Math.round((benchmark-quote)/benchmark*1000)/10;return {flagged:deviation>=threshold,deviation,severity:deviation>=50?"HIGH":deviation>=threshold?"MEDIUM":"LOW"};}
export function discrepancy(reported:number,received:number,threshold=15){const percent=Math.round(Math.abs(reported-received)/reported*1000)/10;return {flagged:percent>threshold,percent,severity:percent>20?"HIGH":"MEDIUM"};}
export function matchScore(x:{distance:number;price:number;maxPrice:number;authorized:boolean;pickup:boolean;reliability:number}){const distance=Math.max(0,100-x.distance*5);const price=x.price/x.maxPrice*100;return Math.round(price*.30+distance*.15+(x.authorized?100:0)*.25+(x.pickup?100:0)*.10+x.reliability*.20);}
export const allowedTransitions:Record<string,string[]>={DRAFT:["CREATED"],CREATED:["QUOTED","CANCELLED"],QUOTED:["ACCEPTED","CANCELLED"],ACCEPTED:["PICKUP_SCHEDULED","CANCELLED"],PICKUP_SCHEDULED:["HANDED_OVER"],HANDED_OVER:["RECEIVED"],RECEIVED:["PAID"],PAID:["RECYCLED"],RECYCLED:[],CANCELLED:[]};
